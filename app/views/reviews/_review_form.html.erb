<% unless current_user.is_a? GuestUser %>
  <%= form_for @review, url: '#' do |form| %>
    <div class="form-group">
      <%= form.label :body %>
      <%= form.text_area :body, class: 'form-control' %>
    </div>

    <%= form.submit 'Post Review', class: 'btn btn-primary' %>
  <% end %>
<% end %>